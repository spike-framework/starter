package app.controller.home;

import Controller from spike.core.Controller;
import Log from spike.core.Log;

class Home extends Controller {

    items: ['a','b','c'],

    Home: function () {

    },

    clickMe: function(context, event){

        console.log(context);
        console.log(event);

    },

    click: function(item, index, x){

        console.log(index + ' : ' + item + ' x: '+x);

    }

}